$("form.prev").after('<%= escape_javascript render @messages.reverse %>');
<% if @messages.next_page %>
	$('form.prev').attr("action", '<%= room_messages_path(params[:room_id], page: @messages.next_page) %>')
<% else %>
	$("form.prev").remove();
<% end %>
